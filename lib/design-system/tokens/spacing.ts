/**
 * App-side re-export of the 4-pt spacing scale. Source of truth
 * lives in `@rowerm8/design-tokens`.
 */
export {
  spacing,
  type SpacingScale,
  type SpacingToken,
} from "@rowerm8/design-tokens";
